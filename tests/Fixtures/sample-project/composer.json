{
    "name": "arcos-cli-tests/sample-project",
    "description": "Minimal real ARCOS project used as a Support-tier fixture for RuntimeBridge/inspect tests. Deliberately has no vendor/ of its own — index.php requires the ARCOS-CLI package's own vendor/autoload.php, which already has pylon/arcos via require-dev.",
    "type": "project",
    "require": {
        "php": ">=8.3"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    }
}
